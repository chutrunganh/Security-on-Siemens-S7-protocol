% Shared verbatim environments (included from main.tex).
\usepackage{fancyvrb}

% Wireshark / protocol dissector dumps (~8pt; still readable when printed).
\DefineVerbatimEnvironment{packetdump}{Verbatim}{%
  fontsize=\scriptsize,
  formatcom=\frenchspacing\ttfamily,
}

% Short shell commands and tool one-liners.
\DefineVerbatimEnvironment{cmddump}{Verbatim}{%
  fontsize=\small,
  formatcom=\frenchspacing\ttfamily,
}
